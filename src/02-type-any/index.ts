// ANY => Qualquer coisa, utilize any só em ultimo caso

// function showMessage(msg: any)  => explicito que quero o any
function showMessage(msg: number) {
  return msg;
}

// console.log(showMessage(12));
